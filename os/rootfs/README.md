# `os/rootfs/`

**Owned by WP-01.** Files copied into the image verbatim. This tree mirrors the image tree.

See `docs/PRD.md` section 8, WP-01, for the deliverables that land here and the
acceptance list they must pass. Do not add files here on behalf of another
work package — one agent per WP (PRD 14.1).
